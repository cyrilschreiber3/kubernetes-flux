apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config:
    address-pools: |
      - name: default
        protocol: layer2
        addresses:
          - 10.1.1.140-10.1.1.159
